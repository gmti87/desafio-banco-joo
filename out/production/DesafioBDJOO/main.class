public class main {

    public static void main(String[] Args) {
        Conta cc = new ContaCorrente();
        Conta poupanca = new ContaPoupanca();

        cc.depositar(100);
        cc.transferir(100, poupanca);

        cc.imprimirExtrato();
        poupanca.imprimirExtrato();

        // Podemos mudar para Econta para usarmos o polimorfismo.
        // Composição você usa quando quer referênciar um objeto dentro de outro.

        /*
        Sugestão de melhorias
        novas classes, mais robustez
        metodo de listar todos clientes,
        podmmos usar o lombok no projeto
        */
    }

}
